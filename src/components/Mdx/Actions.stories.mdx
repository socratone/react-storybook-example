import { Meta, Story, Canvas } from '@storybook/addon-docs';
import Button from './Button';
import { ComponentStory } from '@storybook/react';

export const Template = (args) => <Button {...args} />;

<Meta
  title="MDX/Actions"
  argTypes={{
    children: {
      control: {
        type: 'text',
      },
    },
  }}
  component={Button}
/>

# State

<Canvas>
  <Story name="actions" args={{ children: 'CLICK' }}>
    {Template.bind({})}
  </Story>
</Canvas>
